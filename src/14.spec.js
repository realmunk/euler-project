const eulerFourteen = require("./14");

describe("Euler Project - 14", () => {
  it("should do some stuff ...", () => {
    const collatsSequence = [13, 40, 20, 10, 5, 16, 8, 4, 2, 1];

    expect(eulerFourteen(13)).toBe(collatsSequence);
  });
});
